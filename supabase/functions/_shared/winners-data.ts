/**
 * NOTE: Winners content is members-only.
 *
 * To avoid leaking content via a public GitHub repo, winners data is stored in Supabase
 * (table: public.winners_payload) and served via the `winners` Edge Function after membership checks.
 *
 * This file intentionally contains ONLY placeholders so the repo can remain public.
 */

export const winnersData2025Csv = '';
export const winnersData2025InnovationCsv = '';
export const winnersData2024Csv = '';

// Curated winners JSON placeholder. Real content should be stored in public.winners_payload.
export const winnersExtraJson = '[]';
