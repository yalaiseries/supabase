[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/resources.html"
  to = "/members.html"
  status = 302
  force = true

# This is a multi-page static site (no SPA fallback).
# Optional: if you keep Blogger as an archive subdomain
# [[redirects]]
#   from = "/archive/*"
#   to = "https://blog.aihackathon.pro/:splat"
#   status = 302
