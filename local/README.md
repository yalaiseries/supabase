# Local-only sensitive workspace

Use this folder for files that must stay on your machine and never go to GitHub.

Examples:
- invite retry CSV exports
- temporary auth links
- one-off operational reports with tokens/PII

Notes:
- Everything under this folder is git-ignored by default.
- Only this README and `.gitkeep` are tracked so the folder exists for all collaborators.
